import { Image } from "expo-image";
import search from "../../assets/images/icons/search.png";
const SearchIcon = () => {
  return <Image source={search} style={{ height: 24, width: 24 }} />;
};

export default SearchIcon;
